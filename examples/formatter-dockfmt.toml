# Example generated by ../examples.sh
[formatter.dockfmt]
command = "dockfmt"
excludes = []
includes = [
    "Dockerfile",
    "*/Dockerfile",
    "*.Dockerfile",
    "Dockerfile.*",
    "*/Dockerfile.*",
    "*.dockerfile",
    "dockerfile.*",
    "*/dockerfile.*",
]
options = ["fmt", "-w"]
